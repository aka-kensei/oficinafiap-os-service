# EXEMPLO — crie k8s/secrets.yaml (NÃO versionado) com valores reais em Base64.
# Em produção, use AWS Secrets Manager ou Azure Key Vault.
apiVersion: v1
kind: Secret
metadata:
  name: os-service-secrets
type: Opaque
data:
  # echo -n "<chave-compartilhada-com-Azure-Function-de-auth>" | base64
  Jwt__SecretKey: ""
  # echo -n "Server=oficina-sqlserver.c3sk644cm6im.us-east-2.rds.amazonaws.com;Database=OficinaOS_DB;User Id=admin;Password=<senha>;TrustServerCertificate=True;" | base64
  ConnectionStrings__OSDb: ""
  # echo -n "<senha-rabbitmq>" | base64
  RabbitMq__Password: ""
